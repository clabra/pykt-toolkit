# Core dependencies
numpy>=1.17.2
pandas>=1.1.5
scikit-learn
torch>=1.7.0
wandb>=0.12.9
entmax
tqdm
einops
matplotlib

# Note: tkinter is part of Python standard library but may need system installation on Ubuntu:
# sudo apt update && sudo apt install python3-tk