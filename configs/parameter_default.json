{
  "defaults": {
    "model": "gainakt4",
    "dataset": "assist2015",
    "fold": 0,
    "seed": 42,
    "train_script": "examples/train_gainakt4.py",
    "eval_script": "examples/eval_gainakt4.py",
    "epochs": 30,
    "batch_size": 64,
    "learning_rate": 0.0001,
    "weight_decay": 1.7571e-05,
    "optimizer": "Adam",
    "gradient_clip": 1.0,
    "patience": 4,
    "monitor_freq": 50,
    "use_amp": false,
    "use_wandb": false,
    "auto_shifted_eval": true,
    "seq_len": 200,
    "d_model": 256,
    "n_heads": 4,
    "num_encoder_blocks": 8,
    "d_ff": 1536,
    "dropout": 0.2,
    "emb_type": "qid",
    "lambda_bce": 0.7,
    "lambda_mastery": 0.1,
    "lambda_curve": 0.2,
    "max_attempts": 10,
    "num_trajectories": 10,
    "min_trajectory_steps": 10
  },
  "types": {
    "launcher": [
      "train_script",
      "eval_script"
    ],
    "data": [
      "dataset",
      "fold"
    ],
    "runtime": [
      "seed",
      "epochs",
      "batch_size",
      "learning_rate",
      "weight_decay",
      "optimizer",
      "gradient_clip",
      "patience",
      "monitor_freq",
      "use_amp",
      "use_wandb",
      "auto_shifted_eval",
      "num_trajectories",
      "min_trajectory_steps"
    ],
    "model_config": [
      "seq_len",
      "d_model",
      "n_heads",
      "num_encoder_blocks",
      "d_ff",
      "dropout",
      "emb_type"
    ],
    "loss_weights": [
      "lambda_bce"
    ]
  },
  "fixed": {
    "lambda_constraint": {
      "value": "lambda_mastery = 1.0 - lambda_bce - lambda_curve (when lambda_mastery is null)",
      "rationale": "GAINAKT4 DUAL-ENCODER ARCHITECTURE (2025-11-25): Multi-task loss weights must sum to 1.0. Three losses: L1 (BCE), L2 (Mastery), L3 (Curve). When lambda_mastery is null, it is auto-computed as 1.0 - lambda_bce - lambda_curve for backward compatibility. This ensures proper gradient weighting: L_total = lambda_bce * L1 + lambda_mastery * L2 + lambda_curve * L3. Default lambda_curve=0.0 maintains Phase 1 (single-encoder) behavior.",
      "validated_by_experiments": []
    }
  },
  "md5": "b0892148f3e7922bdc1ef433e55aca2b"
}